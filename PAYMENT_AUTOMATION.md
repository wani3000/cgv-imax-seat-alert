# Toss 결제 자동화 절차

이 문서는 조건 좌석을 확보한 뒤 소유자의 CGV 계정으로 결제를 준비하는 절차다. 휴대폰 번호와 생년월일은 문서, 소스 코드, 환경 변수 또는 Git에 기록하지 않는다. macOS 키체인에서 `payment_identity.py`로 실행 시점에만 읽고 화면 입력에만 사용하며 로그나 알림으로 출력하지 않는다.

## 실행 순서

1. CGV에서 극장, 날짜, 회차, 두 좌석과 총액을 다시 확인한다.
2. 결제수단에서 `toss`를 선택한다.
3. 결제에 필요한 약관을 확인하고 필수 약관에만 동의한다. 선택 마케팅 약관은 동의하지 않는다.
4. `TOSS로 …원 결제하기` 버튼을 눌러 Toss Pay 창을 연다. 이 단계는 결제를 요청할 수 있으므로 현재 사용자 지시가 유효한 예약 흐름에서만 한다.
5. Toss Pay에서 `휴대폰번호` 탭을 선택한다.
6. `payment_identity.get_toss_phone()`과 `payment_identity.get_toss_birthdate()`로 키체인 값을 읽어 각각 휴대폰 번호와 생년월일 6자리 입력란에 채운다.
7. 실제 구매를 확정하거나 Toss 승인 요청을 최종 전송하기 직전에 멈춘다. 사용자에게 극장, 날짜, 회차, 좌석, 총 결제금액을 표시하고 그 시점의 명시적 확인을 받는다.
8. 확인을 받은 뒤에만 최종 버튼을 누른다. CAPTCHA, 추가 본인인증, 앱 승인 또는 오류가 나오면 사용자에게 즉시 알리고 기다린다.

토스 앱에서 알림이 온다는 사실은 최종 결제에 대한 사용자 확인을 대신하지 않는다. 좌석 감시와 Telegram 알림은 CGV 또는 Toss 로그인 상태와 무관하게 계속 수행한다.

## 새 Mac 설정

저장소를 받은 새 Mac에서 `./setup_payment_keychain.sh`를 한 번 실행한다. 값은 입력할 때 화면에 보이지 않으며 해당 Mac의 로그인 키체인에만 저장된다. 이후 `python3 -c 'import payment_identity; payment_identity.get_toss_phone(); payment_identity.get_toss_birthdate()'`로 값 출력 없이 등록 여부를 검사할 수 있다.

