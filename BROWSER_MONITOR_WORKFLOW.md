# CGV 잔여 좌석 감시 절차

감시는 `cgv_iab_monitor.mjs`의 `runCgvIabMonitor(tab, watchDates)`만 사용한다. 좌석 지도에 들어가거나 인원·좌석·결제 항목을 누르지 않는다.

1. `monitor_core.upcoming_watch_dates()`로 가장 가까운 주말부터 3개 주말의 금·토·일과 유효한 예외 날짜를 계산한다.
2. 용산아이파크몰(`0013`)과 천호(`0199`)의 날짜별 직접 URL을 연다.
3. URL의 극장 코드와 날짜가 요청값과 같은지 확인한다.
4. 제목이 정확히 `오디세이`인 영화 아래 `IMAX관` 회차만 읽는다. 패키지·4DX·일반관은 제외한다.
5. 금요일은 19:00 이후, 토·일과 예외일은 전 시간대를 읽는다.
6. 각 회차 버튼의 `잔여석/전체석`을 파싱한다. 좌석 수를 파싱하지 못한 회차가 하나라도 있으면 `complete=false`다.
7. `잔여석 / 전체석 >= 0.5`인 회차만 Telegram 알림에 표시한다. 정확히 50%도 포함한다.
8. 일정이 열리지 않은 날짜는 정상적인 빈 일정으로 처리한다.

`complete=false`일 때는 좌석 현황을 확정하지 말고 확인 장애만 알린다. 감시는 알림 전용이며 로그인·예매·좌석 선택·결제를 수행하지 않는다.
