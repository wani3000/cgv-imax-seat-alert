# CGV 브라우저 감시의 결정적 실행 절차

이 절차는 날짜 버튼의 `토 15` 같은 화면 문구를 이용하지 않는다. 같은 숫자의 날짜가 여러 주차에 표시될 수 있기 때문이다.

## 불변 규칙

1. `monitor_core.upcoming_watch_dates()`로 검사 날짜를 먼저 계산한다.
2. 극장 코드와 `YYYYMMDD`마다 아래 URL을 새로 연다.
   `https://cgv.co.kr/cnm/movieBook/cinema?siteNo={code}&siteNm={name}&scnYmd={YYYYMMDD}`
3. URL의 `siteNo`, `scnYmd`, 화면의 선택 극장·영화·IMAX 표시가 모두 요청값과 일치해야 한다. 하나라도 다르면 해당 범위는 실패다.
4. 영화 제목이 정확히 `오디세이`인 섹션 아래 `IMAX관` 회차만 수집한다. 제목에 오디세이가 포함된 패키지 상영이나 4DX·일반관은 제외한다.
5. 금요일은 시작 시간이 19:00 이상인 회차만 예상 검사 목록에 넣는다.
6. 각 회차를 열면 좌석 화면의 날짜, 극장, `오디세이(IMAX...)`, 선택된 시작 시간을 다시 검증한다.
7. 일반 2명을 선택한 후 전체 좌석 배치와 선택 가능한 좌석을 분리한다. `seatDisabled` 클래스가 있는 좌석은 선택 불가다.
8. `adjacent_target_pairs(available, all_seats=layout, center_radius=15)`로 G~J열 조건 연석을 계산한다.
9. 회차 하나를 끝낼 때마다 검증된 `극장코드|YYYYMMDD|HH:MM` 키를 `checked`에 넣는다.
10. 처음 수집한 `expected`와 `checked`가 완전히 같을 때만 `좌석 있음/없음` 최종 알림을 보낸다.

## 실패 처리

- 날짜, 극장, 영화, 회차 중 하나라도 불일치하면 다른 날짜로 추측하거나 화면 날짜 버튼을 눌러 보정하지 않는다.
- 일부 회차만 검사한 경우 `좌석 없음`을 보내지 않는다. 누락 키와 실패 이유를 로컬 상태에 남기고 Telegram에는 짧은 확인 장애 문구만 한 번 보낸다.
- 회차가 하나도 없는 날짜는 상영 일정 미개방으로 정상 기록하되, 페이지 범위 검증에 성공한 경우에만 그렇게 판단한다.
- 브라우저 뒤로가기에 의존하지 않는다. 다음 회차나 날짜는 검증된 직접 URL에서 다시 시작한다.
